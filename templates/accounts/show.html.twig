{% extends 'base.html.twig' %}

{% block title %}Accounts{% endblock %}

{% block body %}
    <h1>Accounts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ account.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ account.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ account.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('accounts_index') }}">back to list</a>

    <a href="{{ path('accounts_edit', {'id': account.id}) }}">edit</a>

    {{ include('accounts/_delete_form.html.twig') }}
{% endblock %}
